import React from 'react';
import classes from './WebsiteIntro.module.css';

const useWebsiteIntro = () => {
    return (
         <div className={classes.box}>
            <div className={classes.textbox}>
                <div className={classes.text}>当ウェブサイトは。。。</div>
            </div>
            
         </div>
    );
}

export default useWebsiteIntro;